# Known failures in the official MCP conformance suite.
# The runner fails when an entry becomes stale, so fixed scenarios must be
# removed.  The active suite currently passes in full, so there are no expected
# failures.
server: []
